name: package
on:
  push:
    paths:
      - '.github/workflows/package.yml'
      - 'custom-prompt/*'
  workflow_dispatch:

jobs:
  build:
    name: build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-13, macos-14, ubuntu-22.04, windows-2022]
    defaults:
      run:
        working-directory: custom-prompt
    steps:
      - uses: actions/checkout@v4
      - if: runner.os == 'Linux'
        run: sudo apt install libnotify-dev
      - run: make -j release CC=gcc
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}
          path: 'custom-prompt/bin/*'

  release:
    needs: build
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/download-artifact@v4
      - run: |
          chmod +x */*
          ls -Al */*
          file */*
          tar vczf custom-prompt-amd64-macos.tgz macos-13/*
          tar vczf custom-prompt-aarch64-macos.tgz macos-14/*
          tar vczf custom-prompt-amd64-linux.tgz ubuntu-22.04/*
          tar vczf custom-prompt-amd64-linux.tgz windows-2022/*
      - uses: actions/checkout@v4
      - run: |
          ls -Al */*
